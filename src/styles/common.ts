import { CommonStyle } from 'amirisaevui'
import { createGlobalStyle } from 'styled-components'

const CommonStyles = createGlobalStyle`
  ${CommonStyle}
  #root{
    height: 100%;
  }


`

export default CommonStyles
